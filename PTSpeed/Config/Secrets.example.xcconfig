// 复制为 Secrets.xcconfig 后填入真实值；Secrets.xcconfig 已被忽略，不要提交。
// Copia este archivo como Secrets.xcconfig y completa los valores reales; el archivo queda ignorado.
QWEATHER_PRIVATE_KEY = "MC4CAQAwBQYDK2VwBCIEIE/J2HAiPGXdCgaKWj8V9SNWngayd/UVVqKtdZ6wA4EZ"
QWEATHER_PROJECT_ID = "2C88VNJQXF"
QWEATHER_KEY_ID = "KMB2ME5R85"
QWEATHER_HOST = "nj5khxjpk2.re.qweatherapi.com"

// Official YMOBD firmware service; override only with a separately verified staging endpoint.
// Servicio oficial de firmware YMOBD; reemplázalo solo por un endpoint de pruebas verificado por separado.
// YMOBD 官方固件服务；只有确认过独立测试环境后才覆盖此地址。
// EN: `$()` prevents xcconfig from treating the URL's `//` as a comment; Xcode expands it to an empty string.
// ES: `$()` evita que xcconfig trate `//` de la URL como comentario; Xcode lo expande a una cadena vacía.
// 中文：`$()` 用来避免 xcconfig 把 URL 中的 `//` 识别成注释；Xcode 展开后会还原为空字符串。
YMOBD_FIRMWARE_API_BASE_URL = https:/$()/ymobd.com
